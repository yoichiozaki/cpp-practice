#include <bits/stdc++.h>
using namespace std;

int main() {
    long long A, B;
    cin >> A >> B;
    assert(1LL <= A && A <= 5000LL);
    assert(1LL <= B && B <= 5000LL);

    cout << A+B << endl;
    return 0;
}